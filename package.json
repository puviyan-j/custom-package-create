{
  "name": "numbercalculation",
  "version": "1.0.0",
  "description": "crate package",
  "license": "ISC",
  "author": "",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "esbuild index.js --outfile=dist/index.js --bundle --minify",
    "prepare": "npm run build"
  },
  "files": [
    "dist"
  ],
  "dependencies": {
    "esbuild": "^0.25.10"
  }
}
